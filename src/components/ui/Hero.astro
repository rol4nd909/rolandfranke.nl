---
interface Props {
    class?: 'hero--large' | 'text-center' | 'hero--large text-center'
}

const { class: providedProps, ...rest } = Astro.props
---

<header class:list={['hero grid js-angle', providedProps]} id="pageHero" {...rest}>
    <slot />
</header>

<script>
    import { setDiagonalAngle } from '@scripts/diagonalAngle'

    setDiagonalAngle('js-angle')
</script>
